name: Build Secure Windows Proxy

on:
  workflow_dispatch:
  push:
    branches: [ "main" ]

jobs:
  build:
    runs-on: windows-latest

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          # Возвращаем PyArmor в установку
          pip install pyinstaller pyarmor==8.5.10 customtkinter
          pip install -e .[win10]

      - name: Inject Secret Salt
        shell: bash
        run: |
          sed -i "s/REPLACE_ME_SECRET_SALT/${{ secrets.PROXY_SECRET_SALT }}/g" proxy/hwid_auth.py

      - name: Obfuscate Code
        shell: cmd
        run: |
          :: Шифруем только hwid_auth.py
          pyarmor gen -O obf_dist proxy/hwid_auth.py
          
          :: Копируем зашифрованный файл обратно в папку proxy
          copy /Y obf_dist\hwid_auth.py proxy\hwid_auth.py
          
          :: Копируем системную папку PyArmor, необходимую для работы защиты
          xcopy obf_dist\pyarmor_runtime_000000 proxy\pyarmor_runtime_000000 /S /E /I /Y

      - name: Build with PyInstaller
        shell: cmd
        run: |
          :: Собираем EXE. Теперь он захватит и зашифрованный файл, и рантайм
          pyinstaller packaging/windows.spec

      - name: Upload Secure EXE
        uses: actions/upload-artifact@v4
        with:
          name: TGProxy_Secure_Windows
          path: dist/*.exe
